"use client"

interface SectionCategoriesProps {
  categories: string[]
}

export const SectionCategories = ({
    categories
} : SectionCategoriesProps) => {
  return (
     <div>
        SectionCategories
     </div>
    );
}
